# Dependencies
node_modules/
.pnp
.pnp.js

# Build outputs
dist/
.next/
out/
build/
.output/

# Env files — NEVER commit secrets
.env
.env.*
!.env.example
!.env.production.example
!.env.prod.example

# Turbo cache
.turbo/

# OS / Editor
.DS_Store
Thumbs.db
.vscode/settings.json
.idea/
*.swp

# Logs
*.log
npm-debug.log*

# Test artifacts
e2e/.auth/
playwright-report/
e2e-report/
test-results/

# Local data files (xlsx, csv — not committed to git)
DCS\ OPS/
*.xlsx
*.csv
AccountManagement*.xlsx

# Exception: static import template files are public assets
!apps/web/public/import-templates/*.csv

# Source-of-truth archive — read-only reference, never commit (229MB)
/source-of-truth/
/ndma-source-of-truth-*.zip
# Exception: extracted canonical snapshot in docs/ is committed
!docs/source-of-truth/
/category-zips/
/WorkUpdate_*.xlsx
/files.zip
/1.zip

# Claude local settings + ephemeral artifacts (lock files, worktree state, etc.)
.claude/settings.local.json
.claude/*.lock
.claude/worktrees/
.design-bundle/

# local reference / scratch (not for repo)
/.claire/
/design handoff/
ndma hrms*.zip
